<Page
    x:Class="StoreBooking.DonutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoreBooking"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Red">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Style="{StaticResource WhiteIconStyle}" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Original Glazed Count:" Style="{StaticResource TextBlockStyle}"/>
            <Slider Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Maximum="24" Margin="0,0,20,0"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Speedway Special Count:" Style="{StaticResource TextBlockStyle}"/>
            <Slider Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Maximum="24" Margin="0,0,20,0"/>
        </Grid>

    </Grid>
</Page>
